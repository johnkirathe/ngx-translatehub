import { NgxtranslatehubDirective } from './ngxtranslatehub.directive';

describe('NgxtranslatehubDirective', () => {
  it('should create an instance', () => {
    const directive = new NgxtranslatehubDirective();
    expect(directive).toBeTruthy();
  });
});
